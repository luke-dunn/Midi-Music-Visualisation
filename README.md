The controller script is `midi_visual.py`.

Enter the name of the midi file you wish to process in `settings.py`.

The system will then produce a visualisation where the graphics match the midi file and each event results in a visual that aims to represent the structure of the music as video.

The only main dependency is Python `mido` which can be obtained with `pip`
